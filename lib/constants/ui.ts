import type { Accent } from "@/components/ui/kit";

// Selectable accent colours, in picker order.
export const ACCENTS = ["mint", "blue", "amber", "rose", "purple"] as const satisfies readonly Accent[];
